<div class="content-heading">
  Dashboard Active Customers <%= @branch %>
</div>

<div class="row">
  <div class="col-lg-12">
    <!-- START content-->
    <div class="row">
      <div class="col-lg-12">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            Active Customers REPORT
          </div>
          <div class="panel-body">
            <table id="datatable_city" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Brand</th>
                  <th>Active</th>
                  <th>InActive</th>
                </tr>
              </thead>
              <tbody>
                <% @customer.each do |cus| %>
                <tr class="gradeX">
                  <td><%= cus.brand %></td>
                  <td><%= link_to cus.active, request.fullpath.to_s+'?branch='+cus.branch.to_s+'&brand='+cus.brand+'&state=1' %></td>
                  <td><%= link_to cus.inactive, request.fullpath.to_s+'?branch='+cus.branch.to_s+'&brand='+cus.brand+'&state=0' %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <% if params[:state].present? && params[:branch].present? %>
      <div class="col-lg-12">
        <div class="panel panel-info">
          <div class="panel-heading" style="text-align: center">
            List <%= params[:state] == '1' ? 'Active' : 'InActive' %> <%= params[:brand] %> Customers REPORT
          </div>
          <div class="panel-body">
            <table id="datatable_customer" class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Customer</th>
                  <th>Last Order</th>
                </tr>
              </thead>
              <tbody>
                <% @list_customers.each do |cus| %>
                <tr class="gradeX">
                  <td><%= cus.customer %></td>
                  <td><%= cus.last_order.strftime('%d %B %Y') %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <% end %>

    </div>
    <!-- END content-->
  </div>
</div>