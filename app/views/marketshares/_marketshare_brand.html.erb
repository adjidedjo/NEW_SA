<tr>
  <td><%= f.text_field :name, data: { brand_source: @brand.order(:name).map(&:name)} %></td>
  <td><%= f.text_field :amount, label: false %></td>
  <td>
    <%= f.hidden_field :_destroy %>
    <%= link_to 'Delete', '#', class: 'remove_record' %>
  </td>
</tr>